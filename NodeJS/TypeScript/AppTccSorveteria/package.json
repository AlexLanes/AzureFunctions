{
  "name": "apptccsorveteria",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/functions/v*/*.js",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "node --test --test-reporter=spec dist/testes/",
    "pretest": "npm run build"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0-alpha.7",
    "dotenv": "^16.0.3",
    "firebase": "^9.17.2"
  },
  "devDependencies": {
    "@types/node": "^18.x",
    "typescript": "^5.0.4"
  }
}
